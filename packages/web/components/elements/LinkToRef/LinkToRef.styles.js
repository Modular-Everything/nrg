import styled from "styled-components";

export const LinkToRef = styled.span`
  font-family: var(--simplon);
  font-size: 1.6rem;
  line-height: 2.4rem;
  color: var(--nrg-red);
  margin-top: 1.2rem;
  display: inline-flex;

  .arrow {
    margin-left: 0.6rem;
    transition: 250ms ease transform;
  }
`;
