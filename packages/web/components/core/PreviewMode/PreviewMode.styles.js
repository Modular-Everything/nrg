import styled from "styled-components";

export const PreviewMode = styled.div`
  position: absolute;
  top: 0.8rem;
  right: 0.8rem;
  background: var(--nrg-red);
  padding: 0.8rem;
  text-align: center;
  color: var(--nrg-white);
  z-index: 1100;
  font-size: 1.2rem;
`;
